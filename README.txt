Portal
------

A experiment in building a very basic data directory based on CKAN, but requiring only CouchDB.

Installation
------------

1. Install CouchDB
2. Install node 0.4.x & npm
3. Run `portal.js push http://[COUCHSERVER]/[DATABASENAME]`
